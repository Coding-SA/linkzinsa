{
  "dependencies": {
    "gts": "^3.1.0",
    "gulp": "^4.0.2",
    "nodemon": "^2.0.7",
    "ts-node": "^9.1.1"
  },
  "devDependencies": {
    "@types/node": "^14.11.2",
    "gts": "^3.1.0",
    "typescript": "^4.0.3"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "build": "tsc",
    "fix": "gts fix",
    "prepare": "yarn.cmd run build",
    "pretest": "yarn.cmd run build",
    "posttest": "yarn.cmd run lint",
    "dev": "nodemon"
  }
}
